export async function compareCommand(options: { config: string; output: string }): Promise<void> {
  console.log("compare command - not yet implemented");
  console.log(`Config: ${options.config}`);
  console.log(`Output: ${options.output}`);
}
