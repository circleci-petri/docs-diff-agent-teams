export async function authCheckCommand(options: {
  config: string;
}): Promise<void> {
  // Track E implements this (deferred)
  console.log("auth-check command - not yet implemented");
  console.log(`Config: ${options.config}`);
}
